% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_design.R
\name{design_summary}
\alias{design_summary}
\title{S7 design_summary class}
\usage{
design_summary(
  class_label = character(0),
  treatment_col = character(0),
  block_cols = character(0),
  whole_plot_col = character(0),
  sub_plot_col = character(0),
  spatial_cols = character(0),
  scores = integer(0),
  evidence = list(),
  recommended_roles = (function (.data = list(), row.names = NULL) 
 {
     if
    (is.null(row.names)) {
         list2DF(.data)
     }
     else {
         out <-
    list2DF(.data, length(row.names))
         attr(out, "row.names") <- row.names
      
      out
     }
 })(),
  candidates = list(),
  warnings = character(0)
)
}
\description{
Returned by \code{\link[=detect_design]{detect_design()}}. Slots:
}
\details{
\describe{
\item{\code{class_label}}{One of \code{"none"}, \code{"CRD"}, \code{"RCBD"},
\code{"IBD/alpha-lattice"}, \code{"row-column"}, \code{"split-plot"},
\code{"factorial"}.}
\item{\code{treatment_col}}{Character vector — the working treatment
column(s). Length 0 if no treatment was identified; length 2 for
factorial.}
\item{\code{block_cols}}{Character vector — block(s) used by the winning
rule.}
\item{\code{whole_plot_col}, \code{sub_plot_col}}{Split-plot only.}
\item{\code{spatial_cols}}{Length-2 character (\code{row}, \code{col}) if a
spatial pair was detected; otherwise empty.}
\item{\code{scores}}{Named numeric vector — score from every rule.}
\item{\code{evidence}}{Named list — measurements that drove the winning
rule's score.}
\item{\code{recommended_roles}}{Data frame with two columns (\code{col},
\code{role}). Fed into \code{\link[=propose_roles]{propose_roles()}} when \code{detect = TRUE}.}
\item{\code{candidates}}{The candidate set used (for \code{plot()} and
debugging).}
\item{\code{warnings}}{Character vector — non-blocking diagnostics.}
}
}
\keyword{internal}
