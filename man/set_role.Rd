% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/roles_actions.R
\name{set_role}
\alias{set_role}
\title{Set the role and action of one or more columns in a roles table}
\usage{
set_role(roles, cols, role = NULL, action = NULL)
}
\arguments{
\item{roles}{A roles table from \code{\link[=propose_roles]{propose_roles()}} (possibly edited).}

\item{cols}{Character vector of column names to edit. Every entry
must be present in \code{roles$col}.}

\item{role}{Optional single role to assign to all of \code{cols}. One of
\code{"design"}, \code{"treatment"}, \code{"outcome"}, \code{"covariate"}, \code{"date"},
\code{"id"}, \code{"text"}, \code{"other"}.}

\item{action}{Optional single action to assign to all of \code{cols}. One
of \code{"keep"}, \code{"scramble"}, \code{"alias"}, \code{"drop"}. When \code{NULL} (the
default) and \code{role} was supplied, the action is re-resolved to the
default for the new role.}
}
\value{
The edited roles table.
}
\description{
Ergonomic editor for the two-axis roles table returned by
\code{\link[=propose_roles]{propose_roles()}}. Setting a \code{role} without an explicit \code{action}
re-resolves the action to the default for the new role, the column's
kind, and the mode the table was proposed for — so a re-roled column
never silently carries a stale action from its previous role.
}
\details{
Direct edits (\code{roles$role[roles$col == "x"] <- "outcome"}) remain
fully supported; this helper exists because a direct role edit leaves
\code{roles$action} untouched, which is occasionally what you want and
frequently not.
}
\examples{
r <- propose_roles(iris)
r <- set_role(r, "Sepal.Length", role = "outcome")
r <- set_role(r, "Species", action = "keep")
r[, c("col", "role", "action")]

}
\seealso{
\code{\link[=propose_roles]{propose_roles()}}, \code{\link[=roles_validate]{roles_validate()}}.
}
