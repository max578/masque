% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/role_options.R
\name{role_options}
\alias{role_options}
\title{List every role and action combination masque accepts}
\usage{
role_options(kind = NULL)
}
\arguments{
\item{kind}{Optional storage kind to filter for: one of \code{"numeric"},
\code{"integer"}, \code{"factor"}, \code{"character"}, \code{"logical"}, \code{"date"},
\code{"datetime"}, or \code{"other"}. When supplied, only the combinations
workable for a column of that kind are returned. The default \code{NULL}
returns the full grid.}
}
\value{
A tibble with one row per role-action pair and four columns:
\code{role}, \code{action}, \code{kinds} (the storage kinds the pair is valid for:
\code{"all"}, \code{"none"}, \code{"all except other"}, or a comma-separated
list), and \code{notes} (why the pair is constrained, empty when it is
not).
}
\description{
Renders the two-axis vocabulary as a table: every \code{role} paired with
every \code{action}, the storage kinds the pair works for, and the reason
the pair is constrained when it is. The table is generated from the
same compatibility rules \code{\link[=roles_validate]{roles_validate()}} applies at \code{\link[=mask]{mask()}} time,
so what it shows is exactly what a roles table is allowed to say.
}
\details{
A column's \code{kind} is never chosen: \code{\link[=propose_roles]{propose_roles()}} derives it from
the column's class. To move a column to a different kind, convert the
column in the data and re-propose.
}
\examples{
role_options()
role_options(kind = "factor")
subset(role_options(), role == "design")

}
\seealso{
\code{\link[=propose_roles]{propose_roles()}}, \code{\link[=set_role]{set_role()}}, \code{\link[=roles_validate]{roles_validate()}}.
}
