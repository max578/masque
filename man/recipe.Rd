% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recipe.R
\name{recipe}
\alias{recipe}
\title{Extract the recipe from a masque object}
\usage{
recipe(m)
}
\arguments{
\item{m}{A \code{masque} object returned by \code{\link[=mask]{mask()}}.}
}
\value{
A \code{masque_recipe} S7 object.
}
\description{
The recipe is \strong{private}: at least as sensitive as the original data.
Never share alongside the synthetic. By default \code{print(recipe(m))}
redacts all level maps; use \code{\link[=reveal_maps]{reveal_maps()}} for an explicit reveal.
}
\examples{
r <- propose_roles(iris)
r$role[r$col == "Sepal.Length"] <- "outcome"
m <- suppressWarnings(mask(iris, r, seed = 1))
recipe(m)
}
\seealso{
\code{\link[=synthetic]{synthetic()}}, \code{\link[=reveal_maps]{reveal_maps()}}, \code{\link[=mask]{mask()}}.
}
