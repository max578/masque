% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_table.R
\name{clean_table}
\alias{clean_table}
\title{Tidy a dirty table's column names and category labels before masking}
\usage{
clean_table(df, clean = c("auto", "report", "off"), quiet = FALSE)
}
\arguments{
\item{df}{A data frame.}

\item{clean}{One of \code{"auto"} (default - legalise names, trim
whitespace, report near-duplicates), \code{"report"} (legalise names,
report what whitespace / near-duplicate changes \emph{would} be made but
apply none), or \code{"off"} (legalise names only, skip all other
hygiene). Column-name legalisation is applied in \strong{every} mode -- an
invalid name silently rewritten downstream corrupts the clone -- and
is surfaced as a \code{masque_name_repaired} warning; only the whitespace
and near-duplicate handling is governed by the mode.}

\item{quiet}{Logical. When \code{FALSE} (default) a \code{cli} summary of the
fixes and advisories is printed. Set \code{TRUE} to suppress it (the
report object is returned either way).}
}
\value{
An object of class \code{masque_cleaning}: a list with
\itemize{
\item \code{data} - the cleaned (or, under \code{report} / \code{off},
unchanged) data frame;
\item \code{name_map} - named character \code{original -> clean} for every
column whose name changed (empty if none);
\item \code{level_fixes} - named list, one entry per column whose
values were trimmed, each a named character \code{original -> clean};
\item \code{near_duplicates} - a data frame of report-only label pairs
(\code{col}, \code{a}, \code{b}, \code{kind}) that look like typos but were left
untouched;
\item \code{mode} - the \code{clean} mode applied.
}
}
\description{
Real custodian tables arrive with column names that are not valid R
names (\code{"Yield (t/ha)"}, \code{"Site Name"}), leading or trailing
whitespace in names and factor / character values, and the
occasional near-duplicate label (\code{"north"} vs \code{"North"} vs
\code{" north"}). \code{clean_table()} legalises names and trims whitespace, and
reports each fix. Near-duplicate labels are only reported, since merging
labels that look alike is a judgement call; \code{\link[=conform_table]{conform_table()}} makes it.
}
\details{
The corrections are returned alongside the cleaned data, so \code{\link[=mask]{mask()}}
records them in the recipe and \code{\link[=apply_recipe]{apply_recipe()}} re-applies the same
cleaning to the original.
}
\examples{
df <- data.frame(
  `Site Name` = c("north ", "north", "South"),
  `Yield (t/ha)` = c(3.1, 2.9, 5.0),
  check.names = FALSE
)
cl <- clean_table(df, quiet = TRUE)
names(cl$data)
cl$near_duplicates

}
\seealso{
\code{\link[=mask]{mask()}}, \code{\link[=propose_roles]{propose_roles()}}.
}
