% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mask.R
\name{mask}
\alias{mask}
\title{Mask a tabular dataset into a structurally faithful development surrogate}
\usage{
mask(
  df,
  roles,
  mode = c("local", "collaborate"),
  seed = NULL,
  clean = c("auto", "report", "off"),
  alias_names = FALSE,
  conditional = FALSE,
  coords = NULL,
  allow_unmasked_coords = FALSE,
  .shared_maps = list(),
  ...
)
}
\arguments{
\item{df}{A data frame.}

\item{roles}{A roles table from \code{\link[=propose_roles]{propose_roles()}} (possibly edited).
Tables from masque <= 0.5.0 are upgraded with a deprecation
warning; see \code{\link[=roles_validate]{roles_validate()}}.}

\item{mode}{Either \code{"local"} or \code{"collaborate"}. When omitted, inherit
\code{attr(roles, "mode")}, falling back to \code{"local"} for a roles table with
no mode provenance. An explicit collaborate-to-local downgrade raises a
classed \code{masque_mode_downgrade} warning.}

\item{seed}{Optional integer for reproducibility.}

\item{clean}{Label and column-name hygiene before masking, passed to
\code{\link[=clean_table]{clean_table()}}: one of \code{"auto"} (default - trim whitespace and
report near-duplicate labels), \code{"report"} (report only), or \code{"off"}
(skip). Invalid column names are legalised in \strong{every} mode -- an
invalid name silently rewritten during synthesis corrupts the clone --
and the repair is raised as a \code{masque_name_repaired} warning. The
\code{roles} table's column references are remapped to the legalised names,
the fixes are recorded in the recipe, and \code{\link[=apply_recipe]{apply_recipe()}} /
\code{\link[=unmask]{unmask()}} reverse them on the round-trip.}

\item{alias_names}{Hide the column names themselves. \code{FALSE} (the
default) keeps them. \code{TRUE} replaces every retained column name with
an opaque alias (\code{col_001}, \code{col_002}, ... in column order). A
character vector names just the columns to alias. The
original-to-alias map is stored in the recipe and inverted by
\code{\link[=apply_recipe]{apply_recipe()}} / \code{\link[=unmask]{unmask()}}, so a pipeline written against the
aliased synthetic round-trips. Column names are the last identifying
surface a kept or design column exposes; alias them when even the
schema is sensitive.}

\item{conditional}{Logical scalar (default \code{FALSE}). The
\emph{collaborate-grade conditional clone}. When \code{FALSE}, scrambled
numeric columns are re-simulated from one global Gaussian copula -
marginals and global covariance survive, but the treatment-to-outcome
relationship does not, so a causal model fitted on the clone recovers
a null effect. When \code{TRUE}, the numeric block is re-simulated \emph{within
each treatment-by-design stratum}, so a row's synthetic outcome
inherits the location of the treatment that row carries. A causal
model fitted on the conditional clone recovers the real treatment
effect within sampling tolerance - the data-side analogue of
preserving a conditional mean embedding rather than a pooled
marginal. The conditioning columns (treatment plus retained design)
are recorded on the recipe.

The stratum is chosen by a \strong{coarsening ladder}. Treatment crossed
with every retained design column is the finest rung, but on a
replicated factorial that rung holds one row per cell, which is too
thin to synthesise. The ladder then drops design columns, finest
first, until the cells hold at least five rows; treatment columns are
never dropped. Whatever remains below that floor is pooled into a
global fallback. The rung reached is recorded on the recipe as
\code{conditioning_used}, the pooled share as \code{fallback_frac}, and any
coarsening or residual pooling raises a classed
\code{masque_conditional_degraded} warning -- including the case where no
treatment or design column survives at all, in which case the clone is
the pooled copula.}

\item{coords}{Optional geographic-coordinate declaration. Supply one or more
latitude/longitude pairs and each is coarsened in place by an on-land
jitter (see \code{\link[=jitter_coordinates]{jitter_coordinates()}}) instead of being copula-scrambled into
implausible locations. A pair is a named vector \code{c(lat = "lat_col", lon = "lon_col")} or a named list that also carries jitter parameters
(\code{by}, \code{method}, \code{min_km}, \code{max_km}, \code{sd_km}, \code{on_land}); pass several
pairs as a list. Defaults to a donut of 5-20 km on land. One displacement
is drawn per \strong{site} and broadcast to that site's rows: \code{by} names the
columns of \code{df} that identify a site, and is omitted when rows sharing an
identical input coordinate already identify one. A declared pair always
survives masking, coarsened; the recipe records the parameters and the site
grouping it was coarsened under, and \code{\link[=apply_recipe]{apply_recipe()}} retargets to the real
coordinates.}

\item{allow_unmasked_coords}{Single logical. A masked table must not carry
a real coordinate, so by default \code{mask()} \strong{stops} when a column whose
name says coordinate (\code{gps}, \code{lat}, \code{lon}, \code{easting}, ...) would be
written through unmasked, and warns when a column detected only by the
shape of its values would be. State otherwise by declaring the pair to
\code{coords} (coarsened), giving the column a masking action (\code{drop} or
\code{scramble}), or -- having decided the coordinate is not sensitive --
setting this to \code{TRUE}, which is recorded on the recipe.}

\item{.shared_maps}{Internal. A named list of pre-computed
\code{original -> alias} level maps for cross-table linked columns, set
by \code{\link[=mask_set]{mask_set()}}. Not for direct use.}

\item{...}{Must be empty. An unused argument (for example a
misspelled name) errors rather than being silently ignored.}
}
\value{
A \code{masque} S7 object. Use \code{\link[=synthetic]{synthetic()}} and \code{\link[=recipe]{recipe()}} to
extract the components.
}
\description{
Takes one data frame and a user-edited \code{roles} table (from
\code{\link[=propose_roles]{propose_roles()}}, possibly adjusted with \code{\link[=set_role]{set_role()}}) and produces
a synthetic clone according to each column's \code{action}. Returns a
\code{masque} S7 object holding the synthetic data and a private
\code{masque_recipe}.
}
\details{
\code{mode = "local"} marks the synthetic for owner development only; the
reminder is recorded on the recipe and shown when the object prints.
\code{mode = "collaborate"} additionally jitters re-simulated numerics
within their measurement resolution (stochastically rounding
integers) and runs \code{\link[=audit_mask]{audit_mask()}} automatically; a HIGH finding is
raised as a classed warning (\code{masque_high_leakage}) and blocks the
package-managed writers (\code{\link[=masque]{masque()}}'s \code{out}, \code{\link[=write_set]{write_set()}}) until it
is resolved or explicitly overridden. Which columns are aliased,
kept, or dropped is decided by the \code{action} column of \code{roles} -
\code{\link[=propose_roles]{propose_roles()}} resolves mode-appropriate defaults, so the table
you reviewed is the plan that runs.

Collaborate mode adjusts the transformations and runs the audit; it
does not model where the output will go. Whether a synthetic table is
appropriate for a given collaborator, environment, or jurisdiction is
a release decision that stays with the data custodian - masque
informs that decision, it does not make it.
}
\section{Behaviour by action}{


\describe{
\item{\code{keep}}{Byte-identical pass-through, both modes.}
\item{\code{scramble}}{Numeric outcome / covariate columns are
re-simulated jointly via a Gaussian copula on the global Pearson
covariance, with empirical-quantile marginals. Categorical, date,
and text columns are row-permuted within non-NA positions, class
preserved. Treatment columns get a seeded label permutation -
the assignment structure never moves.}
\item{\code{alias}}{As \code{scramble} where applicable, plus opaque label
substitution: treatments become \code{trt_NNN} (\verb{<col>_trt_NNN} when
two or more treatment factors are aliased), categorical
covariates \verb{<col>_LNNN}, design labels \verb{<col>_DNNN} (in place -
structure intact), ids \verb{<col>_INNN} (in place - row linkage
intact), text values \verb{<col>_TNNN}.}
\item{\code{drop}}{Column excluded from the synthetic, both modes.}
}

The NA mask of every retained column is preserved cell-by-cell. RNG
state is preserved across the call.
}

\examples{
r <- propose_roles(iris)
r <- set_role(r, "Sepal.Length", role = "outcome")
m <- mask(iris, r, seed = 1)
head(synthetic(m))

}
\seealso{
\code{\link[=propose_roles]{propose_roles()}}, \code{\link[=set_role]{set_role()}}, \code{\link[=roles_validate]{roles_validate()}},
\code{\link[=synthetic]{synthetic()}}, \code{\link[=recipe]{recipe()}}, \code{\link[=reveal_maps]{reveal_maps()}}.
}
