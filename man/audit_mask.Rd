% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit.R
\name{audit_mask}
\alias{audit_mask}
\title{Audit a masque object for leakage and shareability risks}
\usage{
audit_mask(m, original = NULL, print = TRUE)
}
\arguments{
\item{m}{A \code{masque} object from \code{\link[=mask]{mask()}}.}

\item{original}{Optional. Required when \code{m@audit} is NULL (typically
in local mode). Used to recompute exact-match-pct etc. on demand.}

\item{print}{Logical; if TRUE (default), print a styled report.}
}
\value{
The audit tibble, returned invisibly.
}
\description{
Returns a per-column audit tibble and prints a severity-grouped report.
Auto-runs in \code{mode = "collaborate"} at \code{\link[=mask]{mask()}} time and stores the
result on the masque object (\code{m@audit}); for local-mode audits or
explicit re-audits, pass the original data frame via \code{original}.
}
\details{
Each row of the returned tibble holds:

\itemize{
\item \code{col}: column name in the original.
\item \code{role}: assigned role.
\item \code{kind}: storage kind.
\item \code{leakage_class}: \code{low}, \code{medium}, or \code{high}.
\item \code{n_unique_levels}: distinct non-NA values (categorical only).
\item \code{freq_min}: minimum per-level frequency (categorical only).
\item \code{exact_match_pct}: percentage of synthetic cells equal to the
original cell (numeric only; cell-by-cell).
\item \code{na_pct}: percentage of NA cells in the original column.
\item \code{na_pattern_uniqueness}: fraction of rows in the original with
a globally unique NA pattern (one number per data frame, repeated
on every row).
\item \code{alias_status}: \code{aliased}, \code{passthrough}, or \code{dropped}.
\item \code{notes}: short human summary.
}

Classification heuristics (CODEX-aligned):

\itemize{
\item Retained PII-pattern column -> \code{high}.
\item Treatment unaliased in collaborate -> \code{high}.
\item Categorical covariate with a frequency-1 level in collaborate ->
\code{high}.
\item Outcome with exact-match-pct > 1\\% in collaborate -> \code{medium}.
\item Numeric covariate with exact-match-pct > 5\\% in collaborate ->
\code{medium}.
\item Ignore column retained in local -> \code{low} (informational).
}

Step 7 will lower numeric exact-match-pct under collaborate by adding
within-resolution jitter; until then, expect \code{medium} leakage on
collaborate-mode numerics.
}
\examples{
r <- propose_roles(iris)
r$role[r$col == "Sepal.Length"] <- "outcome"
r$role[r$col == "Species"] <- "covariate"
m <- mask(iris, r, mode = "collaborate", seed = 1)
audit_mask(m)

}
\seealso{
\code{\link[=mask]{mask()}}.
}
