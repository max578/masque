% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conform_table.R
\name{conform_table}
\alias{conform_table}
\title{Bring a cleaned table to an analysable format, reporting every assumption}
\usage{
conform_table(
  df,
  merge_labels = c("report", "auto", "off"),
  types = c("report", "auto", "off"),
  max_levels = 20L,
  clean = c("auto", "report", "off"),
  quiet = FALSE
)
}
\arguments{
\item{df}{A data frame.}

\item{merge_labels}{One of \code{"report"} (default), \code{"auto"} or \code{"off"}.
Governs near-duplicate category labels, and treats the two kinds
differently. A pair differing \strong{only by capitalisation} is one category
whatever happens, so it is merged onto the more frequent spelling, and on a
tie onto whichever appeared first, which is deterministic and recorded. A
pair differing \strong{by one edit} might be two real categories, so it is
merged only when one spelling is clearly the more common; on a tie it is
refused and reported.}

\item{types}{One of \code{"report"} (default), \code{"auto"} or \code{"off"}. Governs
storage format: a low-cardinality character column becomes a factor, a
fully numeric character column becomes numeric, and a fully ISO-8601
character column becomes a \code{Date}.}

\item{max_levels}{Cardinality at or below which a character column is
treated as categorical. Default \code{20}.}

\item{clean}{Passed to \code{\link[=clean_table]{clean_table()}}, which always runs first. Default
\code{"auto"}.}

\item{quiet}{Logical. When \code{FALSE} (default) a \code{cli} summary is printed.}
}
\value{
An object of class \code{masque_conformance}: a list with
\itemize{
\item \code{data} - the conformed data frame;
\item \code{cleaning} - the \code{masque_cleaning} record from \code{\link[=clean_table]{clean_table()}};
\item \code{merges} - one row per near-duplicate pair considered
(\code{col}, \code{from}, \code{to}, \code{n_from}, \code{n_to}, \code{applied}, \code{reason});
\item \code{types} - one row per column whose storage was considered
(\code{col}, \code{from}, \code{to}, \code{applied}, \code{reason});
\item \code{assumptions} - every decision as a plain sentence
(\code{col}, \code{assumption}, \code{applied}), which is the thing to read;
\item \code{modes} - the modes applied.
}
}
\description{
\code{\link[=clean_table]{clean_table()}} makes the fixes that are unambiguously safe: it legalises
column names and trims whitespace, and it \emph{reports} near-duplicate labels
without merging them. \code{conform_table()} is the next step, and it handles the
two jobs that are judgement calls rather than hygiene: deciding that
\code{"NSW"} and \code{"Nsw"} are one category, and deciding that a character column
holding six labels is a factor.
}
\details{
Both are \strong{off by default}. The default \code{"report"} mode names every change
it would make, in plain words, and applies none of them, because a merged
category and a coerced type are decisions about what the data \emph{means} and
masque does not make those silently. Set a mode to \code{"auto"} to apply them,
and the assumption behind each one is recorded in the returned object.

Storage is decided \strong{before} categories, deliberately. A column of numbers
or dates held as text is not a set of categories, and proposing to merge
\code{"4.2"} into \code{"4.4"} because they differ by one character is how an automatic
cleaner destroys data. Only columns that remain categorical are considered
for merging, and a one-character edit between labels shorter than four
characters is ignored, because \code{"a"} and \code{"b"} are one edit apart and are
plainly different things.

What it deliberately does not do: merge two labels that differ by an edit
when neither spelling is the more common, coerce a column whose values do not
all parse, or touch a numeric column's storage. Each is reported as a
\verb{not applied} row with the reason, so the gap is visible rather than silent.
}
\examples{
df <- data.frame(
  state = c("NSW", "Nsw", "NSW", "VIC", "VIC"),
  yield = c("3.1", "2.9", "5.0", "4.2", "3.8"),
  sown  = c("2024-05-01", "2024-05-03", "2024-05-01", "2024-05-08", "2024-05-08"),
  stringsAsFactors = FALSE
)
# report only: nothing is changed
cf <- conform_table(df, quiet = TRUE)
cf$assumptions

# apply the decisions
cf2 <- conform_table(df, merge_labels = "auto", types = "auto", quiet = TRUE)
str(cf2$data)

}
\seealso{
\code{\link[=clean_table]{clean_table()}} for the safe fixes, \code{\link[=propose_roles]{propose_roles()}} for what a
column is, \code{\link[=mask]{mask()}} for the round-trip.
}
