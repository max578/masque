% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mask_set.R
\name{mask_set}
\alias{mask_set}
\title{Mask a multi-table set with cross-table-consistent aliasing}
\usage{
mask_set(
  input,
  roles = NULL,
  links = NULL,
  mode = c("local", "collaborate"),
  seed = NULL,
  clean = c("auto", "report", "off"),
  alias_names = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{input}{A folder path, an \code{.xlsx} / \code{.xls} path, or a named list
of data frames - anything \code{\link[=read_set]{read_set()}} accepts.}

\item{roles}{Optional named list of roles tables, one per input table
(names must match). When \code{NULL} (default), \code{\link[=propose_roles]{propose_roles()}} runs on
each table for the chosen \code{mode}. Supply edited tables for full
control.}

\item{links}{Optional override for link detection: a character vector
of column names to treat as cross-table links, or \code{FALSE} to disable
linking entirely (each table masked independently). When \code{NULL}
(default), links are detected automatically.}

\item{mode}{Either \code{"local"} (default) or \code{"collaborate"}.}

\item{seed}{Optional integer for reproducibility.}

\item{clean}{Hygiene mode passed to \code{\link[=clean_table]{clean_table()}} for every table
(\code{"auto"}, \code{"report"}, or \code{"off"}).}

\item{alias_names}{Hide column names. \code{FALSE} (default) keeps them;
\code{TRUE} aliases every non-link column (linked join keys keep their
names so the synthetic set stays joinable).}

\item{quiet}{Logical; suppress the link / hygiene report.}
}
\value{
A \code{masque_set} S7 object. Use \code{\link[=synthetic]{synthetic()}} for the named list
of synthetic tables and \code{\link[=recipe]{recipe()}} for the private recipe bundle.
}
\description{
The set-level counterpart to \code{\link[=mask]{mask()}}. Takes a folder of tabular
files, an Excel workbook, or a named list of data frames and produces
one synthetic table per input table plus a single private recipe
bundle. Columns that are shared across tables - a site code, a
genotype name, a plot id appearing in several tables - are aliased
\emph{identically everywhere they occur}, so a join written against the
synthetic set still resolves on the masked data.
}
\section{Links}{


A \emph{link} is a column name that appears in two or more tables with a
compatible kind and overlapping values - the join keys of the set.
\code{mask_set()} proposes links automatically and prints them; pass
\code{links} to override. Linked columns are aliased in place with a shared
map (built over the union of values across all tables), so identical
original values map to identical aliases in every table. Set a linked
column's action to \code{keep} in its roles table to pass it through
unmasked instead.
}

\examples{
tables <- list(
  plots = data.frame(
    site = c("A", "A", "B", "B"),
    gen = c("x", "y", "x", "y"),
    yield = c(3.1, 2.9, 4.0, 3.7)
  ),
  sites = data.frame(
    site = c("A", "B"),
    rainfall = c(420, 560)
  )
)
m <- mask_set(tables, mode = "collaborate", seed = 1, quiet = TRUE)
synthetic(m)$plots$site
synthetic(m)$sites$site # same aliases -> the join still works

}
\seealso{
\code{\link[=mask]{mask()}}, \code{\link[=read_set]{read_set()}}, \code{\link[=write_set]{write_set()}}.
}
