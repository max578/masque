% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_set.R
\name{read_set}
\alias{read_set}
\title{Read a set of tables from a folder, an Excel workbook, or a list}
\usage{
read_set(input, sheets = NULL, pattern = NULL)
}
\arguments{
\item{input}{A folder path, an \code{.xlsx} / \code{.xls} path, or a named list
of data frames.}

\item{sheets}{For an Excel workbook, an optional character vector of
sheet names to read (default: all sheets).}

\item{pattern}{For a folder, an optional regular expression to select
files (default: all \code{.csv} / \code{.tsv} / \code{.fst}).}
}
\value{
A named list of data frames.
}
\description{
Ingests a multi-table dataset into a named list of data frames, ready
for \code{\link[=mask_set]{mask_set()}}. Three input shapes are accepted:
}
\details{
\describe{
\item{a folder path}{Every \code{.csv}, \code{.tsv}, and \code{.fst} file in the
folder becomes one table, named by its file name (without
extension). CSV / TSV are read with \code{data.table::fread()}; \code{.fst}
needs the Suggested \code{fst} package.}
\item{an Excel workbook (\code{.xlsx} / \code{.xls})}{Every sheet becomes one
table, named by its sheet name. Needs the Suggested \code{readxl}
package.}
\item{a named list of data frames}{Returned as-is after validation.}
}

Only clean rectangular tables are supported. A sheet or file that does
not read as a rectangle - missing header row (auto-named \code{...1}
columns), zero rows, or zero columns - fails with an explanatory error
naming the offending table. \code{masque} does not attempt to recover
merged cells, multi-row headers, or junk rows; tidy the source first.
}
\examples{
tables <- list(
  plots = data.frame(site = c("A", "B"), yield = c(3.1, 4.2)),
  sites = data.frame(site = c("A", "B"), rainfall = c(400, 550))
)
read_set(tables)

}
\seealso{
\code{\link[=mask_set]{mask_set()}}, \code{\link[=write_set]{write_set()}}.
}
