% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/masque.R
\name{masque}
\alias{masque}
\title{Mask a dataset end to end with one guided call}
\usage{
masque(
  input,
  roles = NULL,
  out = NULL,
  mode = c("local", "collaborate"),
  seed = NULL,
  clean = c("auto", "report", "off"),
  alias_names = FALSE,
  conditional = FALSE,
  ask = interactive(),
  overwrite = FALSE,
  quiet = FALSE,
  allow_high = FALSE
)
}
\arguments{
\item{input}{A data frame, a single tabular file (\code{.csv} / \code{.tsv} /
\code{.fst}), a folder of such files, an Excel workbook, or a named list
of data frames.}

\item{roles}{Optional. A roles table (single-table input) or named
list of roles tables (set input). When supplied, the interactive
review is skipped.}

\item{out}{Optional output path. For a single table, a \code{.csv} file (or
\code{.xlsx} with \code{writexl}). For a set, a folder (one CSV per table) or
an \code{.xlsx} workbook. When \code{NULL} (default) nothing is written.}

\item{mode}{Either \code{"local"} (default) or \code{"collaborate"}.}

\item{seed}{Optional integer for reproducibility.}

\item{clean}{Hygiene mode passed to \code{\link[=clean_table]{clean_table()}} (\code{"auto"},
\code{"report"}, \code{"off"}).}

\item{alias_names}{Hide column names; see \code{\link[=mask]{mask()}} / \code{\link[=mask_set]{mask_set()}}.}

\item{conditional}{Logical scalar (default \code{FALSE}). The conditional
clone mode passed through to \code{\link[=mask]{mask()}} / \code{\link[=mask_set]{mask_set()}}: when \code{TRUE},
numeric columns are re-simulated within each treatment-by-design
stratum so the treatment-to-outcome relationship survives the clone.
See \code{\link[=mask]{mask()}} for the full account.}

\item{ask}{Whether to pause for interactive review when \code{roles} is not
supplied. Defaults to \code{\link[=interactive]{interactive()}}. Set \code{FALSE} to proceed with
the proposed plan without prompting.}

\item{overwrite}{Passed to the writer when \code{out} is set.}

\item{quiet}{Suppress progress messages. Warnings - including the
HIGH-leakage finding - are never suppressed.}

\item{allow_high}{Logical (default \code{FALSE}). When \code{out} is set and
the collaborate-mode audit flagged HIGH leakage, the write is
refused. Pass \code{TRUE} to write anyway after your own review; the
override raises a \code{masque_high_override} warning and is recorded in
the recipe's warnings, so the exception stays auditable.}
}
\value{
A \code{masque} object (single-table input) or a \code{masque_set}
object (set input), invisibly. Use \code{\link[=synthetic]{synthetic()}} and \code{\link[=recipe]{recipe()}}.
}
\description{
The front door. \code{masque()} walks the whole procedure - read the data,
propose column roles, (in an interactive session) pause for you to
review the plan, mask, audit, and optionally write the result - from a
single call. It dispatches on the input: a single file or data frame
goes through \code{\link[=mask]{mask()}}; a folder, an Excel workbook, or a named list of
tables goes through \code{\link[=mask_set]{mask_set()}}.
}
\details{
It is also fully scriptable. Pass an edited \code{roles} table (or named
list of them) to skip the interactive review, and an \code{out} path to
write the masked result in one go. The returned object is the same
\code{masque} / \code{masque_set} you would get from the lower-level verbs, so
anything you can do with those you can do with the result here.
}
\section{The guided flow}{

\enumerate{
\item \strong{Read} the input into one or more clean rectangular tables.
\item \strong{Propose roles} for every column (skipped if you pass \code{roles}).
\item \strong{Review} - in an interactive session with no \code{roles} supplied,
the proposed plan is printed and you are asked to proceed, edit, or
stop. Editing opens the roles table in the spreadsheet editor
(\code{\link[utils:edit]{utils::edit()}}) where the platform provides one; when it cannot
start (for example macOS without XQuartz, or a headless session), a
console editor takes over - pick a column, then a role and an
action from numbered menus, with every change applied through
\code{\link[=set_role]{set_role()}} so default actions re-resolve exactly as on the
scriptable path. With \code{ask = FALSE} (the default in
non-interactive use) the proposed plan is used as-is, with a note.
\item \strong{Mask} the data in the chosen \code{mode}.
\item \strong{Audit} - in \code{collaborate} mode the leakage audit runs and its
headline is printed. A HIGH finding surfaces as a classed warning
(\code{masque_high_leakage}) - it is never silenced by the guided flow.
\item \strong{Write} - if \code{out} is set, the masked data is written there
(mirroring the input format), \emph{unless} the audit left unresolved
HIGH findings: then the write is refused (nothing is written) and
the flagged columns are listed. Resolve them and mask again, or
pass \code{allow_high = TRUE} after your own review - the override is
warned (\code{masque_high_override}) and recorded on the recipe. The
private recipe is never written automatically; persist it yourself
with \code{\link[=save_recipe]{save_recipe()}}.
}
}

\examples{
# Scripted single-table use (no prompt because roles are supplied):
r <- propose_roles(iris)
r <- set_role(r, "Sepal.Length", role = "outcome")
m <- masque(iris, roles = r, seed = 1, ask = FALSE, quiet = TRUE)
head(synthetic(m))

}
\seealso{
\code{\link[=mask]{mask()}}, \code{\link[=mask_set]{mask_set()}}, \code{\link[=propose_roles]{propose_roles()}}, \code{\link[=set_role]{set_role()}},
\code{\link[=write_set]{write_set()}}, \code{\link[=save_recipe]{save_recipe()}}.
}
